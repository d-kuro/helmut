package assert

/*
// OmitMetadata exports the omitMetadata function for testing.
var OmitMetadata = omitMetadata

// ToIgnoreOption foo
func ToIgnoreOption(options ...Option) *ignoreOption {
	opts := &option{}

	for _, o := range options {
		o(opts)
	}

	return opts.ignoreOption
}
*/
